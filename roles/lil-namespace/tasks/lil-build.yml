- tags: always
  include_vars: "{{ item }}"
  with_items:
    - quay-vars.yml
    - lil-vars.yml

- tags: always
  include_vars:
    file: quay-secrets.yml
    name: quay_secrets

- name: "`Secret/{{ quay_puller_robot_account_name }}`"
  kubernetes.core.k8s:
    kubeconfig: "{{ ansible_k8s_kubeconfig }}"
    state: present
    definition:
      apiVersion: v1
      kind: Secret
      metadata:
        name: "{{ quay_puller_secret_name }}"
        namespace: "{{ inventory_namespace }}"
      type: kubernetes.io/dockerconfigjson
      stringData:
        .dockerconfigjson: >-
          {{ lookup("epfl_si.quay.robot_account", quay_organization, quay_puller_robot_account_name,
                                                  token=quay_botfather_token)
          | epfl_si.quay.format_docker_config_json | string }}

- name: "`Secret/{{ quay_builder_secret_name }}`"
  when: >-
    inventory_deployment_stage == "test"
  kubernetes.core.k8s:
    kubeconfig: "{{ ansible_k8s_kubeconfig }}"
    state: present
    definition:
      apiVersion: v1
      kind: Secret
      metadata:
        name: "{{ quay_builder_secret_name }}"
        namespace: "{{ inventory_namespace }}"
      type: kubernetes.io/dockerconfigjson
      stringData:
        .dockerconfigjson: >-
          {{ lookup("epfl_si.quay.robot_account", quay_organization, builder_robot_account_name,
                                                  token=quay_botfather_token)
          | epfl_si.quay.format_docker_config_json | string }}

- name: ServiceAccount/builder
  when: >-
    inventory_deployment_stage == "test"
  kubernetes.core.k8s:
    kubeconfig: "{{ ansible_k8s_kubeconfig }}"
    state: present
    definition:
      apiVersion: v1
      kind: ServiceAccount
      metadata:
        name: builder
        namespace: "{{ inventory_namespace }}"
      secrets:
        - name: "{{ quay_builder_secret_name }}"
      imagePullSecrets:
        - name: "{{ quay_builder_secret_name }}"

# Mirror Node:20 base image here
- name: Mirror the Node:20 base image
  when: >-
    inventory_deployment_stage == "test"
  include_tasks:
    file: ../../tasks/_mirror.yml
  vars:
    mirror_name: "node"
    mirror_from: "docker.io/library/node"
    mirror_tags: ["20"]

- name: Build lil-frontend
  when: >-
    inventory_deployment_stage == "test"
  include_tasks:
    file: _build.yml
  vars:
    build_name: lil-frontend
    build_description: LIL frontend application
    build_base: "{{ ansible_quay_hostname }}/{{ quay_organization }}/node:20"
    build_source:
      type: Git
      git:
        uri: "https://github.com/epfl-si/lil.frontend"
        ref: main
    build_rebuild_tags:
      - lil.rebuild
    build_env: 
      - name: LIL_REACT_APP_AUTH_SERVER_URL
        value: "{{ openid_connect[inventory_environment].base_url }}"
      - name: LIL_REACT_APP_HOMEPAGE_URL
        value: "https://{{ inventory_web_hostname }}/"
      - name: LIL_REACT_APP_GRAPHQL_ENDPOINT_URL
        value: "https://{{ inventory_web_hostname }}/graphql"
      - name: LIL_REACT_APP_ENDPOINT_URL
        value: "https://{{ inventory_web_hostname }}"
      - name: LIL_OIDC_SCOPE
        value: "openid {{ openid_connect[inventory_environment].client_id }}/.default"
      - name: LIL_OIDC_CLIENT_ID
        value: "{{ openid_connect[inventory_environment].client_id }}"

- name: Build lil-backend
  when: >-
    inventory_deployment_stage == "test"
  include_tasks:
    file: _build.yml
  vars:
    build_name: lil-backend
    build_description: lil backend application
    build_base: "{{ ansible_quay_hostname }}/{{ quay_organization }}/node:20"
    build_source:
      type: Git
      git:
        uri: "https://github.com/epfl-si/lil.backend"
        ref: feature/delete-from-rmm
    build_rebuild_tags:
      - lil.rebuild
